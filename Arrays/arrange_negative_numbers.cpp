#include <bits/stdc++.h>
using namespace std;

void arrange(int a[],int n)
{
  //by sorting the array in descending order all the negative elements will come to the left side of array.
	sort(a,a+n);
}
